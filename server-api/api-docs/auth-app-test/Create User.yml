info:
  name: Create User
  type: http
  seq: 1

http:
  method: POST
  url: "{{root}}/users/create"
  body:
    type: json
    data: |-
      {
        "firstName": "Nadeesha",
        "lastName": "Beneragama",
        "email": "nadeesha@gmail.com",
        "password": "Nan_user@123",
        "role": "user"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
