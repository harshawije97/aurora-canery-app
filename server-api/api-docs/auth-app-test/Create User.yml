info:
  name: Create User
  type: http
  seq: 1

http:
  method: POST
  url: "{{root}}/users/create"
  body:
    type: json
    data: |-
      {
        "firstName": "Harshana",
        "lastName": "Wijesinghe",
        "email": "harshawije99@gmail.com",
        "password": "super_admin@123",
        "role": "superadmin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
